export interface SectionCopy {
  readonly title: string;
  readonly description?: string;
}

export interface UiCopy {
  readonly applyNow: string;
  readonly apply: string;
  readonly viewScholarships: string;
  readonly learnMore: string;
  readonly contactUs: string;
  readonly backToScholarships: string;
  readonly backToHome: string;
  readonly home: string;
  readonly tryAgain: string;
  readonly scholarships: string;
  readonly quickLinks: string;
  readonly contact: string;
  readonly loading: string;
  readonly required: string;
  readonly optional: string;
  readonly openNavMenu: string;
  readonly scholarshipProgram: string;
  readonly scholarshipNotFound: string;
}
